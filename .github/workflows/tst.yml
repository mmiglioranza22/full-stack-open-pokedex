name: Testing stuff

on:
  push:
    branches:
      - main

jobs:
  a_test_job:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: github context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"

      - name: commits
        env:
          COMMITS: ${{ toJson(github.event.commits) }}
        run: echo "$COMMITS"

      - name: commit messages
        env:
          COMMITS: ${{ toJson(github.event.commits) }}
          SKIP_WORKFLOW: ${{ contains(toJson(github.event.commits.*.message), '#skip') }}
        run: echo "COMMITS - $COMMITS \n COMMITS_2 - $COMMIT_2 \n joined - $JOIN_COMMIT_MESSAGES \n skip - $SKIP_WORKFLOW"

      - name: Print vars
        env:
          AUTHOR: ${{ toJson(github.event.commits.*.author.username) }}
          COMMIT_URL: ${{ toJson(github.event.commits.*.url) }}
          TIMESTAMP: ${{ toJson(github.event.commits.*.timestamp) }}

        run: echo "AUTHOR - $AUTHOR \n COMMIT_URL - $COMMIT_URL"

      # - name: Discord notification
      #   env:
      #     DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
      #     DISCORD_USERNAME: MrRoboto
      #     # AUTHOR: ${{ toJson(github.event.commits.*.author.username) }}
      #     # COMMIT_URL: ${{ toJson(github.event.commits.*.url) }}
      #     # TIMESTAMP: ${{ toJson(github.event.commits.*.timestamp) }}
      #     # SUCCESS: 40811
      #     # INFO: 16753920
      #     # ERROR: 14423100
      #     DISCORD_EMBEDS: '[{"title":"Commit", "color": 40811, "timestamp": ${{toJson(github.event.commits.*.timestamp)}} }]'
      #   uses: Ilshidur/action-discord@0c4b27844ba47cb1c7bee539c8eead5284ce9fa9
      #   with:
      #     args: "New version deploy success."

      # ,"description":"Author ${{ toJson(github.event.commits.*.author.username) }}",,
      - name: Test Success
        uses: rjstone/discord-webhook-notify@v1.0.2
        env:
          AUTHOR: ${{ toJson(github.event.commits.*.author.username) }}
          COMMIT_URL: ${{ toJson(github.event.commits.*.url) }}
          TIMESTAMP: ${{ toJson(github.event.commits.*.timestamp) }}
        with:
          severity: info
          details: This are details
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}
          username: Test bot
          text: This is a text
          footer: this is a footer
          description: some description
